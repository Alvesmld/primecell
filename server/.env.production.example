# Copie para .env antes do deploy
NODE_ENV=production
PORT=3001

# Gere uma chave forte (ex.: openssl rand -base64 48)
JWT_SECRET=SUBSTITUA_POR_UMA_CHAVE_LONGA_E_ALEATORIA
JWT_EXPIRES_IN=7d

# Opcional: só se o frontend estiver em outro domínio
# CORS_ORIGIN=https://app.seudominio.com
